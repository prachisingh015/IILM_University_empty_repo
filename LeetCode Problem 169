class Solution:
    def majorityElement(self, nums: list[int]) -> int:
        freq = {}
        for num in nums:
            freq[num] = freq.get(num, 0) + 1
        n = len(nums)
        for num, count in freq.items():
            if count > n//2:
                return num
        return None
