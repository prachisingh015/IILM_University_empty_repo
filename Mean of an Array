class Solution:
    def findMean(self, arr):
        # code here 
        n = len(arr)
        totalsum = 0
        for num in arr:
            totalsum += num
            k = (totalsum)//n
        return k
